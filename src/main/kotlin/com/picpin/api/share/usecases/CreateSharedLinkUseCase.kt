package com.picpin.api.share.usecases

import com.picpin.api.verticals.stereotype.UseCase

@UseCase
class CreateSharedLinkUseCase {
/*
    operator fun invoke(): GetMyAlbumResponse.Album {
        // 앨범 소유자 여부 검증

        // 공유 토큰 생성 (식별 값, 만료 기간, 접근 가능한 앨범 id)

        // 링크 응답
    }*/
}
